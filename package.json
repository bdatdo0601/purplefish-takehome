{
  "name": "purplefish-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start:store": "cd persist-store && ./pocketbase serve",
    "start:agent": "cd purplefish-agent-core && npm run dev",
    "start:ui": "cd ui && npm run dev",
    "start": "npm-run-all --parallel start:store start:agent start:ui"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
